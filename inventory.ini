[k0s_controller]
# Local dev — replace with your actual hostname or IP
localhost ansible_connection=local

# Cloud — replace with your node's IP and SSH key path
# 1.2.3.4 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/your-key.pem
