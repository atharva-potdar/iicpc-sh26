apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: build-service
  namespace: platform
  labels:
    {{- include "obarena.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    name: build-service
    kind: Deployment
    apiVersion: apps/v1
  pollingInterval: {{ .Values.kedaScaling.pollingInterval }}
  cooldownPeriod: {{ .Values.kedaScaling.cooldownPeriod }}
  minReplicaCount: {{ .Values.kedaScaling.buildService.minReplicas }}
  maxReplicaCount: {{ .Values.kedaScaling.buildService.maxReplicas }}
  advanced:
    restoreToOriginalReplicaCount: true
  triggers:
    - type: apache-kafka
      metadata:
        bootstrapServers: redpanda.platform.svc.cluster.local:9092
        consumerGroup: {{ .Values.kedaScaling.buildService.consumerGroup }}
        topic: {{ .Values.kedaScaling.buildService.topic }}
        lagThreshold: {{ .Values.kedaScaling.buildService.lagThreshold | quote }}
        allowIdleConsumers: "false"
        offsetResetPolicy: latest
